{
	"name": "memrise-cli",
	"version": "1.0.0",
	"description": "CLI for Memrise teaching",
	"type": "module",
	"bin": {
		"memcli": "dist/index.js",
		"memrise": "dist/index.js"
	},
	"main": "dist/index.js",
	"files": [
		"dist"
	],
	"repository": "dvcrn/memcli",
	"license": "MIT",
	"scripts": {
		"build": "bun build ./index.ts --outfile ./dist/index.js --target node",
		"check": "bunx tsc",
		"prepublishOnly": "bun run check && bun run build"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"commander": "^14.0.3",
		"memrise": "0.2.0"
	}
}
